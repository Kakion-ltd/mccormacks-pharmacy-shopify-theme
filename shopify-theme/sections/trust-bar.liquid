{%- comment -%} The only place the trust strip renders. Add or remove this section per
    template in the editor; it is placed by default on the homepage, product, every
    collection, search and the collections index, above the breadcrumb. {%- endcomment -%}
{% render 'trust-row' %}

{% schema %}
{
  "name": "Trust bar",
  "settings": [],
  "presets": [{ "name": "Trust bar" }]
}
{% endschema %}
